---

    const {src, info, link} = Astro.props;

---

<a href={link} target="_blank" class=" bg-white w-12 h-12 rounded-full flex items-center justify-center">
    <img src={src} alt={info} class="w-8">
</a>

